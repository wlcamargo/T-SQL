-- VARIÁVEL DE TABELA x TABELA TEMPORÁRIA

DECLARE @TABELA TABLE (
	ID INT, 
	NOME VARCHAR(30)
)

INSERT INTO @TABELA VALUES 
(1,'WALLACE'),
(2,'TULIO')
  
SELECT 
	*
FROM 
	@TABELA



IF OBJECT_ID('TEMPDB..#TABELA') IS NOT NULL DROP TABLE #TABELA
GO

CREATE TABLE #TABELA (
	ID INT, 
	NOME VARCHAR(30)
)
GO

INSERT INTO #TABELA VALUES 
(1,'WALLACE'),
(2,'TULIO')

SELECT * FROM #TABELA
GO